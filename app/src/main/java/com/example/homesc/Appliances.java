package com.example.homesc;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;

public class Appliances extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_appliances);
     //   Button swagat=findViewById(R.id.toSwagatAppliances);
       // Button sammy=findViewById(R.id.toSammyArlingtonAppliance);
       // swagat.setOnClickListener(new View.OnClickListener() {
          //  @Override
          //  public void onClick(View view) {
               // startActivity(new Intent(Appliances.this,Vendorhomepage.class));
              //  Intent intent= new Intent(MainActivity.this,Vendorhomepage.class);
             //   startActivity(intent);
 //and same for sammyarligntonappliances
    }
}